<h1>作成ページ</h1>

<%= form_with(model: @task, local: true) do |t| %>
    <%= t.label :content, 'タスク' %>
    <%= t.text_field :content %>
    
    <%= t.submit 'タスクリストに追加' %>
<% end %>

<%= link_to '一覧に戻る', tasks_path %>
